package routines;


public class DQ {

    public static Boolean isNullOrBlank(String value) {
    	if(value==null || "".equals(value)) return true;
    	else return false;
    }
    
    public static Boolean isCorrectGSR_Ref(String value) {
    	if(DQ.isNullOrBlank(value)) return false;
    	
    	String datePattern = "\\w{5}-\\w{4}-\\d{5}";

    	Boolean isCorrect = value.matches(datePattern);
    	Boolean isDate2 = date2.matches(datePattern);

    	System.out.println(isDate1);
    	System.out.println(isDate2);
    	
    	
    	else return false;
    }
}
